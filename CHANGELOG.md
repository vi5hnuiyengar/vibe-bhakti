# Changelog

## v2: four pages (September 2026)

Built to `vibe-bhakti-architecture.md`, phase by phase. `node tools/check.js` passed after every phase.

**Phase 1: shell.** `js/app.js` is split into `app-shell.js` (shared widgets on `VB.UI`, tab bar, hash router), `app-drill.js`, `app-read.js`, `app-script.js` and `app-settings.js`. There are four tabs: अभ्यासः, पठनम्, लिपिः, प्रगतिः. The progress grid moved to प्रगतिः. The tab bar hides during sessions. Tab history is kept one entry deep, so back returns to अभ्यासः and then leaves the site.

**Phase 2: store v2.** New key `vibhakti-abhyasa-v2`. v1 progress is migrated automatically and the v1 key is left in place. `Store.record`, `recall` and `level` take a skill map. `VB.MODULES` is the module registry. Settings gains export and import, and reset now states that it erases everything.

**Phase 3: massed and focused practice.** सरल-अभ्यासः runs ten of one vibhakti, alternating vacana, with four exercise types. अभ्यासं वृणुत runs twelve questions from a chosen set of vibhaktis and classes, and the selection is saved. `Store.planSkills(count, filter)` does the filtering. Both record with `{massed: true}`, which updates memory but never counts toward the unlock gate.

**Phase 3b: pronouns.** Not built. The data files (`pronouns.js`, `pronoun-frames.js`) have not been delivered yet. The store, registry, table and checker already accommodate them.

**Phase 4: script page.** Adds `lipi.js` (delivered data), `lipi-questions.js` (six generators and IAST transliteration) and `app-script.js`. Stages from `VB.LIPI_STAGES` open by the same gate as vibhaktis. There is a letter speed round, and a script grid on प्रगतिः.

**Phase 5: lexicon and frames.** 587 words (from 282) and 276 frames (from 155), dropped in with the one-line `questions.js` change they rely on.

**Phase 6: reading page.** `stories-l1.js`: 48 stories, 285 lines, 554 marked words, every one regenerated by the engine and verified. The reader supports line reveal, three-dropdown answers, correction with the full table, per-line meaning, and completion records.

**Known debt cleared.**
- The unused root `style.css` is removed.
- `fonts/OFL.txt` now ships beside the fonts.
- The live region moved from `#app` to the feedback panel.
- The Python cross-check is committed as `tools/crosscheck.py`. It agrees with the site's engine on all 9,392 forms of the 587 words.

**Not changed:** `js/grammar.js`.
